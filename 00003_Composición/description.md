> Utilizá la función `sumatoria` que programaste en el ejercicio anterior para definir, `promedio :: [Int] -> Int`, que toma una lista de números y calcula el valor promedio (truncado, usando división entera).