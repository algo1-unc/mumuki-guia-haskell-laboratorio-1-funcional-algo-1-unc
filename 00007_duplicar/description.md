Programá la función `duplicar` que dada una lista de números *xs*, devuelve la lista que resulta de duplicar cada valor de *xs*.

>* a) Definila usando **recursión**.
>* b) Definila utilizando la función **map**.